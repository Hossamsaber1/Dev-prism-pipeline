# Flat Structure Architecture

Supported keys:
- renders
- playblasts
- external

Adding a new key requires updates in:

1. Projects.validateFolderStructure()
2. ProjectSettings.validateFolderWidget()
3. MediaProducts.getSimplifiedMediaBasePath()
